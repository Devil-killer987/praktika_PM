<Window x:Class="TecnologApp.Views.OrderDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Производственный заказ" Height="350" Width="500"
        WindowStartupLocation="CenterOwner">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Создание заказа" 
                   FontSize="18" FontWeight="Bold" Margin="0,0,0,20"/>

        <TextBlock Grid.Row="1" Text="Рецептура" Margin="0,0,0,5"/>
        <ComboBox x:Name="cmbRecipe" Grid.Row="2" Height="35" 
                  DisplayMemberPath="product_name" SelectedValuePath="id"
                  Margin="0,0,0,15"/>

        <TextBlock Grid.Row="3" Text="Плановое количество (кг)" Margin="0,0,0,5"/>
        <TextBox x:Name="txtQuantity" Grid.Row="4" Height="35" Margin="0,0,0,15"/>

        <TextBlock Grid.Row="5" Text="Плановая дата запуска" Margin="0,0,0,5"/>
        <DatePicker x:Name="dpStartDate" Grid.Row="6" Height="35" Margin="0,0,0,20"/>

        <StackPanel Grid.Row="7" Orientation="Horizontal" 
                    HorizontalAlignment="Right">
            <Button x:Name="btnSave" Content="Сохранить" Width="100" Height="35"
                    Background="#4CAF50" Foreground="White" Click="BtnSave_Click"/>
            <Button x:Name="btnCancel" Content="Отмена" Width="100" Height="35" 
                    Margin="10,0,0,0" Click="BtnCancel_Click"/>
        </StackPanel>
    </Grid>
</Window>