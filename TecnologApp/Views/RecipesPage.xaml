<Page x:Class="TecnologApp.Views.RecipesPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="RecipesPage"
      Background="#F5F5F5">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Управление рецептурами" 
                   FontSize="28" FontWeight="Bold" Margin="0,0,0,20"/>

        <!-- Фильтры -->
        <Border Grid.Row="1" Background="White" CornerRadius="10" 
                Padding="15" Margin="0,0,0,15">
            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Фильтр по статусу:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                <ComboBox x:Name="cmbStatusFilter" Width="150" Height="30"
                          SelectionChanged="CmbStatusFilter_SelectionChanged">
                    <ComboBoxItem Content="Все" IsSelected="True"/>
                    <ComboBoxItem Content="active"/>
                    <ComboBoxItem Content="draft"/>
                    <ComboBoxItem Content="archived"/>
                </ComboBox>
                <Button x:Name="btnAdd" Content="+ Создать рецептуру" 
                        Width="150" Height="35" Margin="20,0,0,0"
                        Background="#4CAF50" Foreground="White" Click="BtnAdd_Click"/>
                <Button x:Name="btnRefresh" Content="🔄 Обновить" 
                        Width="100" Height="35" Margin="10,0,0,0"
                        Click="BtnRefresh_Click"/>
            </StackPanel>
        </Border>

        <!-- Таблица рецептур -->
        <DataGrid x:Name="dgRecipes" Grid.Row="2" 
                  AutoGenerateColumns="False" 
                  IsReadOnly="True" 
                  SelectionMode="Single"
                  RowHeaderWidth="0"
                  Height="400"
                  AlternatingRowBackground="#F9F9F9">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Width="50" Binding="{Binding id}"/>
                <DataGridTextColumn Header="Продукт" Width="180" Binding="{Binding product_name}"/>
                <DataGridTextColumn Header="Версия" Width="80" Binding="{Binding version}"/>
                <DataGridTextColumn Header="Статус" Width="100" Binding="{Binding status}"/>
                <DataGridTextColumn Header="Сумма %" Width="100" Binding="{Binding sum_percentage}"/>
                <DataGridTextColumn Header="Создана" Width="150" Binding="{Binding created_at}"/>
                <DataGridTemplateColumn Header="Действия" Width="200">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="✏️" Width="35" Margin="2" 
                                        Click="BtnEdit_Click" Tag="{Binding}"/>
                                <Button Content="📋" Width="35" Margin="2" 
                                        Click="BtnComponents_Click" Tag="{Binding}"/>
                                <Button Content="✅" Width="35" Margin="2" 
                                        Click="BtnActivate_Click" Tag="{Binding}"/>
                                <Button Content="🗑️" Width="35" Margin="2" 
                                        Click="BtnDelete_Click" Tag="{Binding}"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Информация о сумме компонентов -->
        <TextBlock x:Name="txtWarning" Grid.Row="3" 
                   Foreground="Red" Margin="0,10,0,0" FontSize="12"/>
    </Grid>
</Page>