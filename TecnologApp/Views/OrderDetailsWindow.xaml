<Window x:Class="TecnologApp.Views.OrderDetailsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Детали заказа" Height="500" Width="700"
        WindowStartupLocation="CenterOwner">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="txtTitle" Text="Детали заказа" 
                   FontSize="18" FontWeight="Bold" Margin="0,0,0,15"/>

        <!-- Информация о заказе -->
        <Border Grid.Row="1" Background="#F0F0F0" CornerRadius="10" 
                Padding="15" Margin="0,0,0,15">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Row="0" Grid.Column="0" Text="Номер заказа:" FontWeight="Bold"/>
                <TextBlock x:Name="txtOrderNumber" Grid.Row="0" Grid.Column="1"/>

                <TextBlock Grid.Row="1" Grid.Column="0" Text="Продукт:" FontWeight="Bold"/>
                <TextBlock x:Name="txtProduct" Grid.Row="1" Grid.Column="1"/>

                <TextBlock Grid.Row="2" Grid.Column="0" Text="Количество:" FontWeight="Bold"/>
                <TextBlock x:Name="txtQuantity" Grid.Row="2" Grid.Column="1"/>

                <TextBlock Grid.Row="3" Grid.Column="0" Text="Статус:" FontWeight="Bold"/>
                <TextBlock x:Name="txtStatus" Grid.Row="3" Grid.Column="1"/>
            </Grid>
        </Border>

        <!-- Список партий -->
        <TextBlock Grid.Row="2" Text="Партии по заказу" FontSize="16" FontWeight="Bold" Margin="0,0,0,10"/>

        <DataGrid x:Name="dgBatches" Grid.Row="3" 
                  AutoGenerateColumns="False" 
                  IsReadOnly="True"
                  AlternatingRowBackground="#F9F9F9">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Номер партии" Width="150" Binding="{Binding batch_number}"/>
                <DataGridTextColumn Header="Статус" Width="120" Binding="{Binding status}"/>
                <DataGridTextColumn Header="Дата запуска" Width="150" Binding="{Binding start_time}"/>
                <DataGridTextColumn Header="Факт. кол-во (кг)" Width="120" Binding="{Binding actual_quantity_kg}"/>
                <DataGridTextColumn Header="Отклонения" Width="100" Binding="{Binding deviation_count}"/>
                <DataGridTemplateColumn Header="Действия" Width="100">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="📋" Width="30" Click="BtnViewBatch_Click" 
                                    Tag="{Binding}" ToolTip="Просмотр партии"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="4" Orientation="Horizontal" 
                    HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button x:Name="btnClose" Content="Закрыть" Width="100" Height="35"
                    Click="BtnClose_Click"/>
        </StackPanel>
    </Grid>
</Window>