<Window x:Class="TecnologApp.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TecnologApp.Views"
        mc:Ignorable="d"
        Title="АгроКонтроль - Вход в систему" 
        Height="550" Width="450" 
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Заголовок -->
        <TextBlock Grid.Row="0" Text="ООО АгроКонтроль" 
                   FontSize="24" FontWeight="Bold" 
                   HorizontalAlignment="Center" Margin="0,0,0,20"/>

        <TextBlock Grid.Row="1" Text="Модуль технолога" 
                   FontSize="14" Foreground="Gray" 
                   HorizontalAlignment="Center" Margin="0,0,0,30"/>

        <!-- Логин -->
        <TextBlock Grid.Row="2" Text="Логин" Margin="0,0,0,5"/>
        <TextBox x:Name="txtUsername" Grid.Row="3" 
                 Height="35" Margin="0,0,0,15" FontSize="14"/>

        <!-- Пароль -->
        <TextBlock Grid.Row="4" Text="Пароль" Margin="0,0,0,5"/>
        <PasswordBox x:Name="txtPassword" Grid.Row="5" 
                     Height="35" Margin="0,0,0,15" FontSize="14"/>

        <!-- Captcha -->
        <StackPanel Grid.Row="6" Orientation="Vertical" Margin="0,0,0,10">
            <TextBlock Text="Введите код с картинки" Margin="0,0,0,5"/>
            <Border BorderBrush="Gray" BorderThickness="1" 
                    CornerRadius="5" Height="70" Margin="0,0,0,10">
                <Image x:Name="imgCaptcha" Stretch="Uniform"/>
            </Border>

            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <TextBox x:Name="txtCaptcha" Width="150" Height="30" 
                         FontSize="14" VerticalContentAlignment="Center"/>
                <Button x:Name="btnRefreshCaptcha" Content="🔄" 
                        Width="40" Height="30" Margin="10,0,0,0" 
                        Click="BtnRefreshCaptcha_Click"/>
            </StackPanel>
        </StackPanel>

        <!-- Кнопки -->
        <StackPanel Grid.Row="7" Orientation="Horizontal" 
                    HorizontalAlignment="Center" Margin="0,70,0,0">
            <Button x:Name="btnLogin" Content="Войти" 
                    Width="120" Height="40" 
                    Background="#2E7D32" Foreground="White"
                    FontSize="14" Click="BtnLogin_Click"/>
            <Button x:Name="btnRegister" Content="Регистрация" 
                    Width="120" Height="40" Margin="10,0,0,0"
                    FontSize="14" Click="BtnRegister_Click"/>
        </StackPanel>

        <!-- Сообщение об ошибке -->
        <TextBlock x:Name="txtError" Grid.Row="8" 
                   Foreground="Red" TextWrapping="Wrap"
                   HorizontalAlignment="Center" Margin="0,10,0,0"/>
    </Grid>
</Window>
