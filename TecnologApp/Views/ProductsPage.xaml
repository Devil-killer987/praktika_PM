<Page x:Class="TecnologApp.Views.ProductsPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Title="ProductsPage"
      Background="#F5F5F5">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Управление продукцией" 
                   FontSize="28" FontWeight="Bold" Margin="0,0,0,20"/>

        <!-- Кнопки действий -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,15">
            <Button x:Name="btnAdd" Content="+ Добавить продукт" 
                    Width="150" Height="35" Background="#4CAF50" 
                    Foreground="White" Click="BtnAdd_Click"/>
            <Button x:Name="btnRefresh" Content="🔄 Обновить" 
                    Width="120" Height="35" Margin="10,0,0,0"
                    Click="BtnRefresh_Click"/>
        </StackPanel>

        <!-- Таблица продуктов -->
        <DataGrid x:Name="dgProducts" Grid.Row="2" 
                  AutoGenerateColumns="False" 
                  IsReadOnly="True" 
                  SelectionMode="Single"
                  RowHeaderWidth="0"
                  AlternatingRowBackground="#F9F9F9">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Width="50" Binding="{Binding id}"/>
                <DataGridTextColumn Header="Код" Width="100" Binding="{Binding code}"/>
                <DataGridTextColumn Header="Наименование" Width="200" Binding="{Binding name}"/>
                <DataGridTextColumn Header="Тип" Width="120" Binding="{Binding product_type}"/>
                <DataGridTextColumn Header="Форма выпуска" Width="120" Binding="{Binding release_form}"/>
                <DataGridTextColumn Header="Статус" Width="100" Binding="{Binding status}"/>
                <DataGridTemplateColumn Header="Действия" Width="150">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal">
                                <Button Content="✏️" Width="30" Margin="2" 
                                        Click="BtnEdit_Click" Tag="{Binding}"/>
                                <Button Content="🗑️" Width="30" Margin="2" 
                                        Click="BtnDelete_Click" Tag="{Binding}"/>
                                <Button Content="📋" Width="30" Margin="2" 
                                        Click="BtnRecipes_Click" Tag="{Binding}"/>
                            </StackPanel>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</Page>